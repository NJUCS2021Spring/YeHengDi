#include <iostream>
#include "process.h"

int main() {
    readWord();
    readGroup();
    /*group testg;
    std::string name;
    for(int i:{1,2,3,4,5}){
        std::cin>>name;
        testg.addMember(word(name));
    }
    group::display(testg);
    group::write(testg);*/
}
